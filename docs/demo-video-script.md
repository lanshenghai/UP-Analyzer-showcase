# 5 分钟演示视频分镜稿

> 总时长：**4:30 – 5:30**。旁白使用中文。  
> 使用 OBS 录制（见 [recording-guide.md](recording-guide.md)）。

---

## 录制前准备

- [ ] 关闭邮件 / IM / 内部聊天
- [ ] 使用合成数据（`ENB_9999` 风格）或[在线交互演示](https://lanshenghai.github.io/UP-Analyzer-showcase/)
- [ ] IDE 工作区使用通用文件夹名，不要用内部路径
- [ ] 显示缩放 125%，分辨率 1920×1080
- [ ] 准备两个窗口：桌面应用（或模拟页）+ Cursor/VS Code

---

## 场景 1 — 问题背景（0:00 – 0:30）

**画面：** 可选片头或出镜；切入杂乱日志文件夹。

**旁白：**
> 在电信基站软件开发中，工程师排障的典型路径是：收集 Snapshot、Trace、Syslog、PCAP 等多种日志，然后在十几个工具之间跳转，手工对齐时间轴，周期往往以周计算。UP-Analyzer 是我从 2018 年开始独立构建的一体化诊断平台，目标是把这条碎片化路径收敛到一个产品里。

**屏幕文字：** `数周 → 数小时` | `日活 1 万` | `8 年，v12+`

---

## 场景 2 — Snapshot 工作流（0:30 – 1:30）

**画面：** 将 snapshot 文件夹拖入应用（或模拟页 Snapshot Tab），展示组件自动发现与导航树。

**操作：**
1. 拖入文件夹 → 进度条
2. 组件树展开（L2、L3、Syslog 节点）
3. 点击某一模块 → 显示解析数据

**旁白：**
> 核心工作流是 Snapshot 一站式入口：拖入日志包，平台自动发现组件、选择对应版本的解析器、生成结构化数据。用户不需要知道底层有 500 多个 Python 脚本，也不需要手动配置 Python 环境——运行时已经捆绑好了。

**高亮：** 嵌入式 Python、「500+ 解析脚本」标注

---

## 场景 3 — 可视化与 KPI（1:30 – 2:30）

**画面：** 切换到 KPI 仪表盘；展示 OK vs NOK 对比图。可快闪 Trace/Beam 可视化。

**操作：**
1. 打开 KPI Tab
2. 选择两个构建版本（OK / NOK）
3. 图表显示计数器差异（约 −5.9% 的合成数据）
4. 快速切到 Trace 时间轴视图

**旁白：**
> 解析结果不只是表格。KPI 对比、时序图表、跨版本 diff 都在同一个界面完成。这张图展示的是一个典型的回归场景：升级后某个计数器下降了约 6%，但问题出在哪里，还需要进一步调查。

**高亮：** 计数器名为合成数据（`COUNTER_AVAIL_CH`）

---

## 场景 4 — IDE 集成（2:30 – 4:00）

**画面：** 切换到 Cursor 或 VS Code，展示斜杠命令菜单与 Agent 查询解析数据。

**操作：**
1. 展示已安装插件
2. 输入 `/` → 选择查询命令
3. Agent 读取 parser 注册表，对 localhost 执行 curl
4. 展示含 parsed module 路径的 JSON 响应
5. Agent 在聊天中总结发现

**旁白：**
> 2026 年的关键演进是把平台嵌进开发者每天使用的 IDE。插件会注册本机正在运行的解析实例，AI Agent 通过 curl 直接查询结构化数据，不需要在工具和 IDE 之间来回切换。这不是套了一个 ChatUI，而是让 AI 能「看见」真实的工程数据。

**高亮：** `parser_servers/*.json` 注册表（路径可打码）、`curl --noproxy`

---

## 场景 5 — 分阶段 AI 调查（4:00 – 5:00）

**画面：** 运行调查命令，或播放预录的 Stage A→E 输出滚动。

**操作：**
1. 输入调查命令 + 合成问题 ID
2. 展示阶段推进：工作集 → 量化 → 调用点 → 机制 → 审查
3. 以结构化报告结束（可参考 [调查报告样例](../examples/sample-investigation-report.md)）

**旁白：**
> 对于回归类问题，我设计了一套分阶段调查流水线：每个阶段有确定性 Gate，没有证据就不能进入下一步。最后经过对抗式审查，输出带候选 commit 的结构化报告。这把原来依赖专家经验的调查路径，变成了可重复执行的工程流程。

**高亮：** `HOLDS` 裁决、`invocation gap`、候选 commit 哈希（合成）

---

## 场景 6 — 总结（5:00 – 5:30）

**画面：** README 中的架构图；指标表。

**旁白：**
> 八年持续演进，6000 多次提交，日活约一万。从个人脚本到跨平台产品，再到 AI 原生排障工作流。这个项目证明的不只是全栈能力，而是能把领域经验产品化、工程化、智能化。

**片尾文字：**
```
作者：兰生海
GitHub：github.com/lanshenghai
邮箱：lanshenghai@126.com
```

**结束：** 静默 3 秒 → 淡出

---

## 备选 B -roll

若无法录制生产环境应用：

| 场景 | 替代方案 |
|------|----------|
| 2–3 | 浏览器全屏打开[在线演示](https://lanshenghai.github.io/UP-Analyzer-showcase/) |
| 4 | IDE 中粘贴模拟 curl 输出录屏 |
| 5 | 在 VS Code 中滚动 `examples/sample-investigation-report.md` |

---

## 后期制作

1. 添加中文字幕（可选英文字幕）
2. 导出：H.264，1080p，8–15 Mbps
3. 上传 B 站（国内）+ YouTube（海外，可设不公开）
4. 更新 README 演示视频链接
5. 导出时截取 5 张图存入 `assets/screenshots/`
